# Base URL of the CommitIt backend API (must include the /api/v1 prefix).
# Defaults to http://localhost:8000/api/v1 if unset — see src/services/api/apiClient.ts.
#VITE_API_URL=http://localhost:8000/api/v1
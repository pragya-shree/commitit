/**
 * Theme barrel export.
 *
 * Import design tokens from here — `import { brand, gradients, duration }
 * from "@/theme"` — rather than reaching into individual files, so call
 * sites don't need to know how the token system is internally split up.
 */

export * from "./colors";
export * from "./typography";
export * from "./layout";
export * from "./motion";

/**
 * Hero barrel export.
 *
 * `import { Hero } from "@/components/hero"` — the landing page's hero
 * section. Sub-components (RepositoryUrlForm, HeroVisual) are exported
 * too in case a later screen needs one in isolation.
 */

export * from "./Hero";
export * from "./RepositoryUrlForm";
export * from "./HeroVisual";
